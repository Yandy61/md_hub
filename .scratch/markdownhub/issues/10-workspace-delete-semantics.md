# 10 — workspace 新建与删除语义

**What to build:** 管理员在固定 workspace 目录（config 可配）里新建 md 文件并自动加入索引；删除语义完整落地：服务新建的文件删除 = 取消索引 + 删除真实文件；外部索引条目删除 = 仅取消索引。UI 文案明确区分"取消共享"与"删除"，防止误删。

**Blocked by:** 08 — 索引管理（增/删/源丢失）

**Status:** ready-for-agent

- [ ] 新建文件落在 workspace 内、自动入索引、访客可见
- [ ] 删除服务新建的文件：索引移除且真实文件被删除
- [ ] 删除外部条目：仅取消索引，原文件完好；两种操作的 UI 文案可区分
- [ ] 非管理员无法新建/删除
- [ ] HTTP seam 测试覆盖两种删除语义与 workspace 边界（不能建在 workspace 外）
